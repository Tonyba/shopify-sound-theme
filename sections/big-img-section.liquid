{{ 'big-img-section.css' | asset_url | stylesheet_tag }}

<div class="container">
    <div class="section-wrapper-margin">
        <div class="flex align-item-center column-mobile gradient-bg">
            <div class="w-65 w-100-mobile">
                <h2>{{ section.settings.heading }}</h2>
                <p>{{ section.settings.text }}</p>
            </div>
            <div class="w-35 w-100-mobile">
                {{- section.settings.image | image_url: width: section.settings.image.width | image_tag: loading: 'lazy', decoding: 'async' , draggable: false -}}
            </div>
        </div>
    
    </div>

  
</div>

{% schema %}
    {
        "name": "Big Image Section",
        "class": "big-img-section",
        "settings": [
            {
                "type": "image_picker",
                "id": "image",
                "label": "Image"
            },
            {
                "type": "text",
                "id": "heading",
                "label": "Heading"
            },
            {
                "type": "textarea",
                "id": "text",
                "label": "Text"
            }
        ],
        "presets": [
            {
              "name": "Big Image Section"
            }
        ]
    }
{% endschema %}
